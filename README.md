## got manually todo to get testApp running
* run build clibs script
* build sambag prj
* copy cairo-MT-debug.dll to debug folder cairo.dll
* set vs working directory to root directory (in order to find images correctly)